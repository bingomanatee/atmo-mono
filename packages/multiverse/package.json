{
  "name": "@wonderlandlabs/multiverse",
  "version": "0.2.0",
  "private": false,
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/types/index.d.ts",
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:watch": "vitest --watch",
    "test:ui": "vitest --ui",
    "build:types": "node scripts/generate-dts.js",
    "build:all": "yarn build && yarn build:types",
    "prepublishOnly": "yarn build:all"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^5.0.0",
    "vitest": "^3.1.3"
  }
}